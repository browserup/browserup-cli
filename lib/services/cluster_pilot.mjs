import DockerClient from "./docker_client.mjs";

export class ClusterPilot {
    createCluster({ clusterParams, clusterName }) {
    }

    destroyCluster({ clusterName, region = null, removeVolumes = true }) {

    }

    upgradeCluster(credentials) {
    }
}
