"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTransforms = void 0;
const fs_1 = require("fs");
const path_1 = require("path");
const getTransforms = () => (0, fs_1.readdirSync)((0, path_1.join)(__dirname, "..", "transforms"), { withFileTypes: true })
    .filter((dirent) => dirent.isDirectory())
    .map((dirent) => dirent.name)
    .map((dirName) => {
    const { name, description, options } = require(`../transforms/${dirName}/transform`); // eslint-disable-line
    return { name, description, options };
});
exports.getTransforms = getTransforms;
