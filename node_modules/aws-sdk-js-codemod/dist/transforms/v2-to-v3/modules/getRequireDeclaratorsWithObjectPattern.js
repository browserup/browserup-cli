"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getRequireDeclaratorsWithObjectPattern = void 0;
const config_1 = require("../config");
const getRequireDeclarators_1 = require("./getRequireDeclarators");
const getRequireDeclaratorsWithObjectPattern = (j, source, { identifierName, sourceValue }) => (0, getRequireDeclarators_1.getRequireDeclarators)(j, source, sourceValue).filter((declarator) => {
    if (declarator.value.id.type !== "ObjectPattern") {
        return false;
    }
    const { properties } = declarator.value.id;
    return properties.some((property) => {
        if (!config_1.OBJECT_PROPERTY_TYPE_LIST.includes(property.type))
            return false;
        const propertyValue = property.value;
        return propertyValue.type === "Identifier" && propertyValue.name === identifierName;
    });
});
exports.getRequireDeclaratorsWithObjectPattern = getRequireDeclaratorsWithObjectPattern;
