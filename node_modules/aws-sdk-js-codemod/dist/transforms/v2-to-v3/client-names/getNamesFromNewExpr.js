"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getNamesFromNewExpr = void 0;
const utils_1 = require("../utils");
const getNamesFromNewExpr = (j, source, v2GlobalName) => [
    ...source
        .find(j.NewExpression, (0, utils_1.getClientNewExpression)({ v2GlobalName }))
        .nodes()
        .map((newExpression) => newExpression.callee.property.name),
    ...source
        .find(j.NewExpression, (0, utils_1.getDocClientNewExpression)({ v2GlobalName }))
        .nodes()
        .map((newExpression) => newExpression.callee.object
        .property.name),
];
exports.getNamesFromNewExpr = getNamesFromNewExpr;
