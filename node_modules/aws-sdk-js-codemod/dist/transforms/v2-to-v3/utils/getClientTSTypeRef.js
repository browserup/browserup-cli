"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getClientTSTypeRef = void 0;
const getClientTSTypeRef = ({ v2ClientLocalName, v2ClientName, v2GlobalName, withoutRightSection = false, }) => {
    if (!v2GlobalName && !v2ClientLocalName) {
        throw new Error(`One of the following options must be provided: v2ClientLocalName, v2GlobalName`);
    }
    if (v2GlobalName && v2ClientLocalName) {
        throw new Error(`Only one of the following options must be provided: v2ClientLocalName, v2GlobalName`);
    }
    if (v2GlobalName) {
        const idWithGlobalName = {
            type: "TSQualifiedName",
            left: { type: "Identifier", name: v2GlobalName },
            right: { type: "Identifier", name: v2ClientName },
        };
        return {
            typeName: {
                ...(withoutRightSection ? { left: idWithGlobalName } : idWithGlobalName),
            },
        };
    }
    const idWithClientName = { type: "Identifier", name: v2ClientLocalName };
    return {
        typeName: {
            ...(withoutRightSection ? { left: idWithClientName } : idWithClientName),
        },
    };
};
exports.getClientTSTypeRef = getClientTSTypeRef;
