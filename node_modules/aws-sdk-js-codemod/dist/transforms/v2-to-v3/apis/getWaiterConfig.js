"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getWaiterConfig = void 0;
const config_1 = require("../config");
const getWaiterConfig = (callArgument) => {
    if (callArgument.type !== "ObjectExpression") {
        return;
    }
    for (const property of callArgument.properties) {
        if (!config_1.OBJECT_PROPERTY_TYPE_LIST.includes(property.type)) {
            continue;
        }
        const propertyKey = property.key;
        const propertyValue = property.value;
        if (propertyKey.type !== "Identifier" || propertyValue.type !== "ObjectExpression") {
            continue;
        }
        if (propertyKey.name === "$waiter") {
            return propertyValue;
        }
    }
};
exports.getWaiterConfig = getWaiterConfig;
